a = [{ 1: "A" }, { 1: "C", 2: "A" }, { 2: "C" }];
function vrati(a) {
  var lista = [];
  for (var i of a) 
    if (a[i].Object.keys(object).length == 1) 
        lista.push(a[i]);
return lista;
}
console.log(vrati(a));
